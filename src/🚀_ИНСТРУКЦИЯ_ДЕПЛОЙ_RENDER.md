# 🚀 Пошаговая инструкция: Деплой Backend на Render

**Дата:** 11 ноября 2025  
**Репозиторий:** https://github.com/Mila3699/my-backend.git  
**Время выполнения:** ~15 минут

---

## ✅ ЧТО УЖЕ ГОТОВО

Ваш backend код **отличный** и полностью готов к деплою! 

✅ Google Gemini AI интеграция  
✅ Multer для загрузки видео  
✅ CORS настройки  
✅ Error handling  
✅ Подробные логи  
✅ Health check endpoints

---

## 📋 НУЖНЫЕ ФАЙЛЫ В ВАШЕМ РЕПОЗИТОРИИ

Убедитесь, что в вашем `my-backend` репозитории есть следующие файлы:

### 1. `server.js` ✅
У вас уже есть этот файл - он идеален!

### 2. `package.json` ✅
У вас уже есть - всё правильно!

### 3. `.env.example` ⚠️ Нужно создать

Создайте файл `.env.example` в корне репозитория:

```env
# Google AI API Key
# Получить: https://aistudio.google.com/app/apikey
GOOGLE_AI_API_KEY=your_api_key_here

# Порт сервера (Render установит автоматически)
PORT=10000

# URL фронтенда для CORS
# Локально: http://localhost:5173
# Продакшен: https://your-app.vercel.app
FRONTEND_URL=*
```

### 4. `.gitignore` ⚠️ Нужно создать

Создайте файл `.gitignore`:

```gitignore
# Environment variables
.env
.env.local

# Dependencies
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS files
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# Logs
logs/
*.log

# Temporary files
tmp/
temp/
```

### 5. `README.md` (опционально, но рекомендуется)

Создайте файл `README.md`:

```markdown
# AI Reels Scripter - Backend

Backend сервер для анализа коротких видео через Google Gemini AI.

## 🚀 Деплой на Render

1. Создайте Web Service на Render
2. Подключите этот репозиторий
3. Настройте Environment Variables (см. .env.example)
4. Deploy!

## 🔑 Environment Variables

- `GOOGLE_AI_API_KEY` - Ключ от Google AI Studio
- `PORT` - Порт сервера (автоматически от Render)
- `FRONTEND_URL` - URL фронтенда для CORS

## 📡 Endpoints

- `GET /` - Проверка работы сервера
- `GET /api/health` - Детальная информация
- `POST /api/analyze` - Анализ видео

## 🛠 Локальная разработка

```bash
npm install
cp .env.example .env
# Отредактируйте .env и вставьте ваш API ключ
npm start
```

Server: http://localhost:3001
```

---

## 📤 ШАГ 1: Загрузите файлы в GitHub

### 1.1. Откройте терминал в папке my-backend

```bash
cd /path/to/my-backend
```

### 1.2. Создайте недостающие файлы

**Создайте .env.example:**
```bash
# Mac/Linux:
cat > .env.example << 'EOF'
GOOGLE_AI_API_KEY=your_api_key_here
PORT=10000
FRONTEND_URL=*
EOF

# Windows (создайте вручную через блокнот)
```

**Создайте .gitignore:**
```bash
# Mac/Linux:
cat > .gitignore << 'EOF'
.env
.env.local
node_modules/
npm-debug.log*
.DS_Store
.vscode/
.idea/
*.log
EOF

# Windows (создайте вручную через блокнот)
```

### 1.3. Закоммитьте и запушьте

```bash
git add .
git commit -m "Подготовка к деплою на Render"
git push origin main
```

**✅ Результат:** Все файлы в GitHub репозитории

---

## 🌐 ШАГ 2: Подключите Render к GitHub

### 2.1. Войдите в Render

```
1. Откройте https://dashboard.render.com
2. Войдите в ваш аккаунт
```

### 2.2. Подключите GitHub (если еще не подключили)

```
1. Dashboard → Account Settings (иконка профиля справа вверху)
2. В левом меню → "Connected Accounts"
3. Найдите GitHub → нажмите "Connect"
4. Разрешите Render доступ к GitHub
5. Выберите "Only select repositories"
6. Выберите репозиторий: "my-backend"
7. Нажмите "Install & Authorize"
```

**✅ Результат:** Render видит ваш my-backend репозиторий

---

## 🚀 ШАГ 3: Создайте Web Service на Render

### 3.1. Создайте новый сервис

```
1. Render Dashboard → "New +" (синяя кнопка справа вверху)
2. Выберите "Web Service"
3. В списке репозиториев найдите "my-backend"
4. Нажмите "Connect"
```

### 3.2. Заполните настройки

#### **Основные настройки:**

```
Name: ai-reels-backend
   (можно выбрать любое имя, это будет в URL)

Region: Frankfurt (EU Central)
   ⚡ Ближе к России - быстрее работает

Branch: main
   (или master, если у вас так называется)

Root Directory: (оставьте пустым)

Runtime: Node

Build Command: npm install

Start Command: node server.js
```

#### **Instance Type:**

```
🆓 Free
   - Бесплатно
   - 512 MB RAM
   - Засыпает после 15 минут бездействия
   - Холодный старт ~30 секунд
   
💰 Starter ($7/месяц) - РЕКОМЕНДУЕТСЯ
   - 512 MB RAM
   - Всегда активен
   - Без холодных стартов
   - Поддержка 24/7
```

**Рекомендация:** Начните с Free, потом можете апгрейдиться.

---

## 🔐 ШАГ 4: Настройте Environment Variables

### 4.1. Откройте настройки переменных

Прокрутите вниз до раздела **"Environment Variables"** или нажмите **"Advanced"**.

### 4.2. Добавьте переменные

Нажмите **"Add Environment Variable"** для каждой переменной:

#### **Переменная 1: GOOGLE_AI_API_KEY**

```
Key: GOOGLE_AI_API_KEY
Value: [ВАШ КЛЮЧ ОТ GOOGLE AI STUDIO]
```

**Как получить ключ (если забыли):**
```
1. Откройте https://aistudio.google.com/app/apikey
2. Войдите в Google аккаунт
3. Нажмите "Create API key"
4. Выберите существующий проект или создайте новый
5. Скопируйте ключ (формат: AIzaSyB...)
```

#### **Переменная 2: PORT**

```
Key: PORT
Value: 10000
```

**Примечание:** Render может игнорировать это и использовать свой порт, но лучше указать.

#### **Переменная 3: FRONTEND_URL**

```
Key: FRONTEND_URL
Value: *
```

**Примечание:** После деплоя frontend измените на конкретный URL (например, `https://ai-reels-scripter.vercel.app`)

#### **Переменная 4: NODE_ENV (опционально)**

```
Key: NODE_ENV
Value: production
```

### 4.3. Проверьте

У вас должно быть **3-4 переменных**:
- ✅ GOOGLE_AI_API_KEY
- ✅ PORT
- ✅ FRONTEND_URL
- ✅ NODE_ENV (опционально)

---

## 🎬 ШАГ 5: Запустите деплой!

### 5.1. Нажмите "Create Web Service"

```
1. Прокрутите вниз
2. Нажмите синюю кнопку "Create Web Service"
3. Render начнет деплой
```

### 5.2. Смотрите логи

Вы увидите логи в реальном времени:

```
==> Cloning from https://github.com/Mila3699/my-backend...
==> Checking out commit abc123...
==> Running build command 'npm install'...
npm install
added 67 packages in 12s
==> Build successful!
==> Starting service with 'node server.js'...

==================================================
🚀 AI REELS SCRIPTER - BACKEND
==================================================
📡 Сервер запущен: http://localhost:10000
🌐 Разрешенный frontend: *
🔑 API ключ: ✅ Настроен
==================================================

💡 Эндпоинты:
   GET  /           - Проверка работы
   GET  /api/health - Детальная проверка
   POST /api/analyze - Анализ видео

✨ Готов к работе!
```

### 5.3. Дождитесь завершения

Процесс займет **2-5 минут**. Вы увидите:

```
✅ Build successful
✅ Deploy live
```

---

## 🎉 ШАГ 6: Получите URL вашего backend

### 6.1. Скопируйте URL

В верхней части страницы Render вы увидите URL типа:

```
https://ai-reels-backend.onrender.com
```

Или:

```
https://ai-reels-backend-xxxx.onrender.com
```

**Скопируйте этот URL!** Он нужен для frontend.

### 6.2. Проверьте работу

Откройте в браузере ваш URL:

```
https://ai-reels-backend.onrender.com/
```

Должен вернуться JSON:

```json
{
  "status": "ok",
  "service": "AI Reels Scripter Backend",
  "version": "1.0.0",
  "timestamp": "2025-11-11T12:34:56.789Z",
  "uptime": 5.432,
  "endpoints": {
    "analyze": "POST /api/analyze"
  }
}
```

**Если видите это - ВСЁ РАБОТАЕТ!** ✅✅✅

### 6.3. Проверьте health endpoint

Откройте:

```
https://ai-reels-backend.onrender.com/api/health
```

Должен вернуться:

```json
{
  "status": "ok",
  "checks": {
    "apiKey": "configured",
    "memory": "45.23 MB",
    "uptime": "0.15 минут"
  },
  "timestamp": "2025-11-11T12:35:00.000Z"
}
```

**Если `"apiKey": "configured"` - Google AI ключ настроен правильно!** ✅

---

## 🔄 ШАГ 7: Обновите Frontend для работы с Render

Теперь нужно подключить ваш frontend к новому backend.

### 7.1. Сохраните URL backend

```
Мой backend URL: https://ai-reels-backend.onrender.com
```

### 7.2. Дайте мне знать!

**Напишите:**
- ✅ Backend задеплоен на Render
- ✅ URL: [ваш URL]
- ✅ Health check работает

**И я сразу обновлю frontend код для подключения к вашему backend!**

---

## 📊 Полезная информация о Render

### Логи

```
1. Render Dashboard → ваш сервис
2. Вкладка "Logs"
3. Здесь видны все console.log из вашего кода
```

### Перезапуск сервиса

```
1. Render Dashboard → ваш сервис
2. Справа вверху: "Manual Deploy" → "Clear build cache & deploy"
```

### Изменение Environment Variables

```
1. Render Dashboard → ваш сервис
2. Вкладка "Environment"
3. Редактируйте переменные
4. Сохраните (сервис перезапустится автоматически)
```

### Апгрейд плана

```
1. Render Dashboard → ваш сервис
2. Вкладка "Settings"
3. "Instance Type" → выберите Starter ($7/мес)
4. Подтвердите
```

---

## 🐛 Решение проблем

### ❌ Build failed

**Проверьте:**
- `package.json` есть в репозитории
- Все зависимости указаны правильно
- Build Command: `npm install`

### ❌ Deploy failed

**Смотрите логи:**
- Может не хватать памяти (апгрейд до Starter)
- Проверьте Start Command: `node server.js`

### ❌ "apiKey": "missing"

**Решение:**
1. Render Dashboard → Environment
2. Проверьте GOOGLE_AI_API_KEY
3. Убедитесь что нет пробелов
4. Сохраните и дождитесь перезапуска

### ❌ CORS ошибки

**Решение:**
1. Пока оставьте FRONTEND_URL=*
2. После деплоя frontend обновите на конкретный URL

### ❌ Сервис "засыпает" (Free tier)

**Объяснение:**
- Free tier Render засыпает после 15 минут бездействия
- Первый запрос после сна займет ~30 секунд (холодный старт)

**Решения:**
1. Апгрейд до Starter ($7/мес) - рекомендуется
2. Настроить ping каждые 10 минут (cron job)
3. Предупредить пользователей о задержке

---

## ✅ ЧЕКЛИСТ

Убедитесь, что выполнено:

- [ ] Все файлы (server.js, package.json, .env.example, .gitignore) в GitHub
- [ ] Render подключен к GitHub
- [ ] Web Service создан
- [ ] Environment Variables настроены (GOOGLE_AI_API_KEY, PORT, FRONTEND_URL)
- [ ] Деплой успешен
- [ ] GET / возвращает JSON со статусом
- [ ] GET /api/health показывает "apiKey": "configured"
- [ ] URL backend скопирован

---

## 🎯 СЛЕДУЮЩИЙ ШАГ

**После того как backend задеплоен:**

1. Скопируйте URL вашего backend
2. Дайте мне знать
3. Я обновлю frontend код для работы с вашим Render backend
4. Задеплоим frontend на Vercel
5. Протестируем полный flow с реальным Google AI!

**Готовы продолжить?** 🚀

---

## 📞 Нужна помощь?

Если что-то не получается:

1. **Скопируйте текст ошибки** из Render Logs
2. **Сделайте скриншот** проблемного места
3. **Дайте мне знать** - я помогу решить!

Успехов! 💪
