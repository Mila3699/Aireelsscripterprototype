# 🔍 Как это работает?

## Простым языком

Представьте, что вы хотите отправить письмо другу через посредника (потому что вы не можете встретиться напрямую).

```
ВЫ → ПОСРЕДНИК → ДРУГ → ПОСРЕДНИК → ВЫ
```

В нашем случае:
- **ВЫ** = Ваш браузер (веб-приложение)
- **ПОСРЕДНИК** = Backend сервер
- **ДРУГ** = Google AI (Gemini)

---

## Схема работы

```
┌─────────────────┐
│   ВАШ БРАУЗЕР   │  ← Вы видите красивый интерфейс
│  (Frontend)     │  ← Загружаете видео
└────────┬────────┘
         │
         │ 1. Загружает видео
         ▼
┌─────────────────┐
│  BACKEND СЕРВЕР │  ← Node.js сервер на вашем компьютере
│  (localhost)    │  ← Хранит API ключ в безопасности
└────────┬────────┘
         │
         │ 2. Отправляет видео в Google AI
         ▼
┌─────────────────┐
│   GOOGLE AI     │  ← Gemini анализирует видео
│   (Gemini)      │  ← Создает сценарий и рекомендации
└────────┬────────┘
         │
         │ 3. Возвращает результат
         ▼
┌─────────────────┐
│  BACKEND СЕРВЕР │  ← Получает ответ от Google
└────────┬────────┘
         │
         │ 4. Отправляет результат в браузер
         ▼
┌─────────────────┐
│   ВАШ БРАУЗЕР   │  ← Показывает красивые результаты!
└─────────────────┘
```

---

## Почему нужен Backend?

### ❌ Почему нельзя напрямую?

```javascript
// ОПАСНО! НЕ ДЕЛАЙТЕ ТАК!
const API_KEY = "AIzaSyB_секретный_ключ"
// Этот ключ виден всем в браузере!
```

Если вы используете API ключ прямо в браузере:
- 🚨 Любой человек может открыть код и украсть ключ
- 🚨 С вашим ключом могут потратить ваши деньги
- 🚨 Могут заблокировать ваш аккаунт

### ✅ Правильно с Backend

```javascript
// Backend (безопасно)
const API_KEY = process.env.GOOGLE_AI_API_KEY // Секрет хранится на сервере

// Frontend (публично)
fetch('http://localhost:3001/api/analyze', {...}) // Ключа тут нет!
```

Backend хранит секреты, а Frontend только показывает результаты.

---

## Что происходит шаг за шагом

### Шаг 1: Загрузка видео (Frontend)

```typescript
// В вашем браузере
const formData = new FormData();
formData.append('video', file); // Добавляем видео

fetch('http://localhost:3001/api/analyze', {
  method: 'POST',
  body: formData // Отправляем на backend
});
```

**Что происходит:**
- Вы перетащили видео в приложение
- Браузер отправляет его на ваш backend (localhost:3001)

---

### Шаг 2: Получение видео (Backend)

```javascript
// На вашем сервере (server.js)
app.post('/api/analyze', upload.single('video'), async (req, res) => {
  const videoFile = req.file; // Получили видео
  console.log('📹 Получено видео:', videoFile.originalname);
  
  // Дальше отправляем в Google AI...
});
```

**Что происходит:**
- Backend получил видео
- Проверил, что это действительно видео
- Готовится отправить в Google AI

---

### Шаг 3: Отправка в Google AI (Backend)

```javascript
// Backend отправляет в Google
const model = genAI.getGenerativeModel({ model: 'gemini-1.5-flash' });

const result = await model.generateContent([
  {
    inlineData: {
      mimeType: 'video/mp4',
      data: videoBase64 // Видео в формате base64
    }
  },
  { text: ANALYSIS_PROMPT } // Наш промпт с инструкциями
]);
```

**Что происходит:**
- Backend конвертирует видео в base64 (текстовый формат)
- Отправляет в Google Gemini вместе с промптом
- Промпт говорит AI, что именно анализировать

---

### Шаг 4: Google AI анализирует

```
Google Gemini:
1. Смотрит видео
2. Слушает аудио
3. Транскрибирует речь
4. Анализирует визуал
5. Создает сценарий
6. Генерирует рекомендации
7. Возвращает JSON с результатами
```

**Время:** 30-60 секунд (в зависимости от длины видео)

---

### Шаг 5: Возврат результата (Backend)

```javascript
// Backend получил ответ от Google
const response = await result.response;
const text = response.text(); // JSON в виде строки

// Парсим JSON
const analysisData = JSON.parse(text);

// Отправляем в браузер
res.json(analysisData);
```

**Что происходит:**
- Backend получил анализ от Google
- Проверил, что это правильный JSON
- Отправляет обратно в ваш браузер

---

### Шаг 6: Показ результатов (Frontend)

```typescript
// В браузере
const result = await response.json();
setAnalysisResult(result); // Обновляем интерфейс
setAppState('results'); // Показываем страницу с результатами
```

**Что происходит:**
- Браузер получил результат
- Показывает красивую страницу с анализом
- Вы можете скопировать сценарий

---

## Технические детали

### Форматы данных

**Видео → Backend:**
```
Content-Type: multipart/form-data
video: [binary video file]
```

**Backend → Google AI:**
```javascript
{
  inlineData: {
    mimeType: "video/mp4",
    data: "base64_encoded_video..."
  },
  text: "промпт с инструкциями..."
}
```

**Google AI → Backend:**
```json
{
  "original": {
    "transcription": "...",
    "translation": "..."
  },
  "keys": [...],
  "script": [...],
  "recommendations": [...]
}
```

**Backend → Frontend:**
```json
{
  "original": {...},
  "keys": [...],
  "script": [...],
  "recommendations": [...],
  "_metadata": {
    "processingTime": "35.42",
    "fileSize": "8.50",
    "fileName": "video.mp4"
  }
}
```

---

## Безопасность

### ✅ Что безопасно:

1. **API ключ на backend** - никто не может его увидеть
2. **CORS настройки** - только ваш сайт может обращаться к backend
3. **Валидация файлов** - принимаем только видео
4. **Лимиты размера** - максимум 100 МБ

### ⚠️ На что обратить внимание:

1. **Не коммитьте .env** - добавьте в .gitignore
2. **Используйте HTTPS** на продакшене
3. **Добавьте rate limiting** - ограничение количества запросов
4. **Удаляйте временные файлы** - чтобы не забить диск

---

## Альтернативные архитектуры

### Вариант 1: С облачным хранилищем

```
Frontend → Backend → Google Cloud Storage → Google AI → Backend → Frontend
```

**Плюсы:**
- Большие файлы
- Параллельная обработка
- История видео

### Вариант 2: Serverless

```
Frontend → Cloud Function → Google AI → Frontend
```

**Плюсы:**
- Не нужен сервер 24/7
- Автоматическое масштабирование
- Платите только за использование

**Минусы:**
- Сложнее настроить
- Cold start (задержка первого запроса)

---

## Стек технологий

| Компонент | Технология | Зачем нужна |
|-----------|-----------|------------|
| Frontend | React + TypeScript | Интерфейс |
| Стилизация | Tailwind CSS | Красивый дизайн |
| UI компоненты | shadcn/ui | Готовые компоненты |
| Backend | Node.js + Express | Сервер-посредник |
| Загрузка файлов | Multer | Обработка видео |
| AI | Google Gemini 1.5 Flash | Анализ видео |
| База данных | (опционально) | Хранение результатов |

---

## Производительность

### Время обработки:

| Длина видео | Размер | Время обработки |
|------------|--------|----------------|
| 15 сек | 5 МБ | ~20 сек |
| 30 сек | 10 МБ | ~35 сек |
| 60 сек | 20 МБ | ~60 сек |
| 180 сек | 50 МБ | ~120 сек |

### Оптимизация:

1. **Используйте gemini-1.5-flash** вместо pro (в 2 раза быстрее)
2. **Сжимайте видео** перед загрузкой
3. **Ограничивайте длину** (рекомендуется < 60 сек)
4. **Кешируйте результаты** для повторных запросов

---

## FAQ

### В: Можно ли обойтись без backend?

**О:** Технически да, но небезопасно. API ключ будет виден всем в коде браузера.

### В: Сколько стоит Google AI?

**О:** Бесплатный план: 15 запросов/минуту. Платный: ~$0.00001875 за 1000 символов.

### В: Где хранятся видео?

**О:** В текущей версии - в памяти backend (не сохраняются на диск). Можно добавить сохранение в облако.

### В: Можно ли использовать другой AI?

**О:** Да! Замените Google AI на OpenAI GPT-4V, Claude с vision, или другой.

---

## Что дальше?

1. ✅ Запустите базовую версию (следуйте `ЧЕКЛИСТ_ЗАПУСКА.md`)
2. 📊 Добавьте базу данных для истории анализов
3. 👥 Добавьте авторизацию пользователей
4. 💰 Добавьте платные планы
5. 🚀 Задеплойте на продакшен

---

**Теперь вы понимаете, как всё устроено!** 🎓

Если остались вопросы, посмотрите:
- [ИНСТРУКЦИЯ_ДЛЯ_НОВИЧКОВ.md](./ИНСТРУКЦИЯ_ДЛЯ_НОВИЧКОВ.md)
- [FAQ.md](./docs/FAQ.md)
