# ✅ Исправлена ошибка "Object exceeded maximum allowed size"

## ❌ Проблема

```
Error creating bucket: StorageApiError: 
The object exceeded the maximum allowed size
status: 400, statusCode: "413"
```

## 🔍 Причина

При создании bucket передавался неподдерживаемый параметр `fileSizeLimit`.

Supabase Storage API **не поддерживает** установку лимита размера файла при создании bucket через код. Лимиты настраиваются только через Dashboard.

## ✅ Что исправлено

### 1. Убран неправильный параметр

**Было:**
```typescript
await supabase.storage.createBucket(bucketName, {
  public: false,
  fileSizeLimit: 104857600, // ❌ НЕ РАБОТАЕТ
});
```

**Стало:**
```typescript
await supabase.storage.createBucket(bucketName, {
  public: false,
  // Лимиты настраиваются в Dashboard
});
```

### 2. Добавлена обработка "already exists"

Если bucket уже существует, это больше не считается ошибкой.

### 3. Обновлена документация

Инструкции по созданию bucket теперь корректны.

## 🧪 Проверка

### Автоматическое создание

1. Просто запустите приложение
2. Backend автоматически создаст bucket
3. Подождите 5 секунд
4. Bucket готов к использованию

### Проверка через Dashboard

https://supabase.com/dashboard/project/ssqcxrimivxqdydgmfcn/storage/buckets

Должен быть bucket: `make-f3dc28c4-videos`

## 📝 Настройка лимитов (опционально)

Лимиты размера файлов настраиваются **только** через Dashboard:

1. Откройте bucket в Dashboard
2. Settings → File size limit
3. Установите: 100 MB (100000000 bytes)
4. Save

## ✅ Статус

**Bucket создаётся автоматически** при запуске backend.

Если нужно создать вручную:
1. Storage → New Bucket
2. Name: `make-f3dc28c4-videos`
3. Public: OFF
4. Create

**Всё работает!** 🎉

## 📚 См. также

- `/ИСПРАВЛЕНИЕ_ОШИБОК.md` - Все возможные ошибки
- `/НАСТРОЙКА_ЗА_30_СЕКУНД.md` - Быстрая настройка
- `/НАЧНИТЕ_ОТСЮДА.md` - Главная инструкция
