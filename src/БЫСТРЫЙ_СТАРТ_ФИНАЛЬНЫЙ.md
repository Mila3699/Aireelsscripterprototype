# 🚀 Быстрый старт за 2 минуты

## ✅ Шаг 1: Email Confirmation (30 сек)

https://supabase.com/dashboard/project/ssqcxrimivxqdydgmfcn/settings/auth

→ Disable "Enable email confirmations" → Save

---

## ✅ Шаг 2: Storage Bucket (30 сек)

**Автоматически:** Подождите 5 секунд - создастся сам.

**Вручную (если не создался):**

https://supabase.com/dashboard/project/ssqcxrimivxqdydgmfcn/storage/buckets

→ New Bucket → Name: `make-f3dc28c4-videos` → Public: OFF → Create

---

## ✅ Шаг 3: Storage Policies (1 мин)

**ВАЖНО:** Обязательный шаг!

### SQL Editor:

https://supabase.com/dashboard/project/ssqcxrimivxqdydgmfcn/sql/new

### Скопируйте и выполните:

```sql
-- Загрузка
CREATE POLICY "Users can upload their own videos"
ON storage.objects FOR INSERT TO authenticated
WITH CHECK (
  bucket_id = 'make-f3dc28c4-videos' 
  AND (storage.foldername(name))[1] = auth.uid()::text
);

-- Просмотр
CREATE POLICY "Users can view their own videos"
ON storage.objects FOR SELECT TO authenticated
USING (
  bucket_id = 'make-f3dc28c4-videos'
  AND (storage.foldername(name))[1] = auth.uid()::text
);

-- Удаление
CREATE POLICY "Users can delete their own videos"
ON storage.objects FOR DELETE TO authenticated
USING (
  bucket_id = 'make-f3dc28c4-videos'
  AND (storage.foldername(name))[1] = auth.uid()::text
);

-- Обновление
CREATE POLICY "Users can update their own videos"
ON storage.objects FOR UPDATE TO authenticated
USING (
  bucket_id = 'make-f3dc28c4-videos'
  AND (storage.foldername(name))[1] = auth.uid()::text
)
WITH CHECK (
  bucket_id = 'make-f3dc28c4-videos'
  AND (storage.foldername(name))[1] = auth.uid()::text
);
```

→ **Run** → Должно быть: `Success. No rows returned`

---

## 🎉 Готово!

Теперь протестируйте:

1. Email: test@test.com / Password: test123
2. → Зарегистрироваться
3. → Загрузить видео
4. → Получить демо-результат

✅ **Всё работает!**

---

## 🆘 Если ошибки

| Ошибка | Решение |
|--------|---------|
| Invalid credentials | Шаг 1 не выполнен |
| Bucket not found | Шаг 2 не выполнен |
| RLS policy violation | Шаг 3 не выполнен |

**Подробно:** `/FIX_RLS_ERROR.md`

---

## 📚 Полная документация

- **RLS ошибка:** `/FIX_RLS_ERROR.md`
- **SQL политики:** `/STORAGE_POLICIES.sql`
- **Все ошибки:** `/ИСПРАВЛЕНИЕ_ОШИБОК.md`
- **Главная:** `/НАЧНИТЕ_ОТСЮДА.md`
