# 🔐 Настройка Supabase Auth

## ⚠️ ВАЖНО: Настройка Email Confirmation

Для работы регистрации и входа нужно настроить Supabase:

### 1. Откройте Supabase Dashboard
```
https://supabase.com/dashboard/project/ssqcxrimivxqdydgmfcn
```

### 2. Перейдите в Authentication → Email Templates

### 3. Отключите Email Confirmation (для прототипа)

**Вариант А: Отключить подтверждение email (рекомендуется для прототипа)**

1. Перейдите в **Settings** → **Authentication**
2. Найдите **"Enable email confirmations"**
3. **Выключите** эту опцию
4. Нажмите **Save**

**Вариант Б: Настроить SMTP (для production)**

1. Перейдите в **Settings** → **Authentication** → **SMTP Settings**
2. Включите **"Enable Custom SMTP"**
3. Настройте SMTP сервер (Gmail, SendGrid и т.д.)
4. Сохраните настройки

## 🔧 Как работает текущая реализация

### Регистрация (Frontend)
```typescript
// lib/supabase.ts
1. supabase.auth.signUp({ email, password })
   ↓
2. POST /auth/confirm-email (backend с admin правами)
   ↓
3. admin.updateUserById({ email_confirm: true })
   ↓
4. Автоматический вход через signIn(email, password)
```

### Вход (Frontend)
```typescript
// lib/supabase.ts
1. supabase.auth.signInWithPassword({ email, password })
   ↓
2. Проверка credentials
   ↓
3. Создание session
   ↓
4. Возврат access_token
```

## 🐛 Решение проблемы "Invalid login credentials"

Если появляется эта ошибка, проверьте:

### 1. Email подтверждён?
```sql
-- Запустите в Supabase SQL Editor:
SELECT id, email, email_confirmed_at, created_at 
FROM auth.users 
WHERE email = 'your@email.com';
```

Если `email_confirmed_at` = NULL, email не подтверждён.

**Решение:**
```sql
-- Подтвердите email вручную:
UPDATE auth.users 
SET email_confirmed_at = NOW() 
WHERE email = 'your@email.com';
```

### 2. Пользователь существует?
```sql
-- Проверьте существование:
SELECT * FROM auth.users WHERE email = 'your@email.com';
```

Если пользователя нет - зарегистрируйтесь заново.

### 3. Правильный пароль?
- Убедитесь что вводите тот же пароль что и при регистрации
- Минимум 6 символов
- Без лишних пробелов

### 4. Очистите базу и начните заново
```sql
-- ВНИМАНИЕ: Удаляет всех пользователей!
DELETE FROM auth.users;
```

Затем зарегистрируйтесь снова.

## 📝 Тестовая последовательность

### Шаг 1: Регистрация
```
1. Email: test@example.com
2. Password: test123
3. Confirm: test123
4. → Нажать "Зарегистрироваться"
```

**Ожидаемый результат:**
- ✅ Toast: "Регистрация успешна! Вход выполнен"
- ✅ Переход на главный экран
- ✅ В консоли: "Пользователь создан", "Email подтверждён", "Вход успешен"

### Шаг 2: Выход
```
1. Нажать кнопку "Выйти" (правый верхний угол)
```

**Ожидаемый результат:**
- ✅ Toast: "Вы вышли из системы"
- ✅ Переход на экран входа

### Шаг 3: Вход
```
1. Email: test@example.com
2. Password: test123
3. → Нажать "Войти"
```

**Ожидаемый результат:**
- ✅ Toast: "Вход выполнен успешно!"
- ✅ Переход на главный экран
- ✅ В консоли: "Попытка входа", "Вход успешен"

## 🔍 Отладка

### Проверьте консоль браузера (F12)

**При регистрации должны быть логи:**
```
Регистрация пользователя: test@example.com
Пользователь создан: <user-id>
Email подтверждён
Выполняем вход после регистрации...
Попытка входа: test@example.com
Вход успешен: <user-id>
```

**При входе должны быть логи:**
```
Попытка входа: test@example.com
Вход успешен: <user-id>
```

**Если ошибка:**
```
Sign in error: Invalid login credentials
```

→ Значит email не подтверждён или неверный пароль.

## 🚀 Production настройки

Для production приложения:

1. **Включите Email Confirmation**
   - Users получат письмо с ссылкой подтверждения
   - Настройте email templates в Supabase

2. **Настройте SMTP**
   - Используйте профессиональный SMTP сервис
   - SendGrid, Mailgun, AWS SES и т.д.

3. **Добавьте "Forgot Password"**
   - Используйте `supabase.auth.resetPasswordForEmail()`
   - Настройте redirect URL

4. **Уберите backend endpoint /auth/confirm-email**
   - Он нужен только для прототипа
   - В production используйте email confirmation

## ✅ Текущий статус

- ✅ Регистрация через frontend (Supabase SDK)
- ✅ Автоподтверждение email через backend
- ✅ Автоматический вход после регистрации
- ✅ Вход через frontend (Supabase SDK)
- ✅ Проверка сессии при загрузке
- ✅ Выход из системы
- ✅ Детальное логирование

## 🆘 Если ничего не помогает

1. Удалите пользователя из базы:
```sql
DELETE FROM auth.users WHERE email = 'your@email.com';
```

2. Отключите Email Confirmation в настройках Supabase

3. Перезагрузите страницу (Ctrl+F5)

4. Зарегистрируйтесь заново

5. Проверьте консоль на ошибки
