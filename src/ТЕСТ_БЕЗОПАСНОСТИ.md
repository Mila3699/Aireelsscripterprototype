# 🧪 Инструкция по тестированию безопасности

## Быстрые тесты

Выполните эти тесты, чтобы убедиться, что все меры безопасности работают корректно.

---

## ✅ 1. Тест UTF-8 и кириллицы

### Что тестируем:
- Корректное отображение русского текста
- Правильная загрузка шрифтов

### Как тестировать:
1. Откройте приложение в браузере
2. Проверьте заголовок "AI Reels Scripter"
3. Проверьте текст "Проанализируй вирусный рилс и получи готовый сценарий на русском"

### ✅ Результат:
- Весь русский текст четкий и читаемый
- Нет иероглифов или квадратиков
- Шрифт выглядит современно и профессионально

### Инструменты разработчика:
```javascript
// Откройте Console (F12)
window.getComputedStyle(document.body).fontFamily
// Должно содержать 'Inter'
```

---

## ✅ 2. Тест поддержки браузеров

### Что тестируем:
- Работу приложения на разных браузерах
- Корректную сборку для старых устройств

### Как тестировать:
1. Откройте приложение в разных браузерах:
   - ✅ Chrome (последняя версия)
   - ✅ Firefox (последняя версия)
   - ✅ Safari (на Mac/iPhone)
   - ✅ Edge (последняя версия)

2. Проверьте на мобильных:
   - ✅ iOS Safari (iPhone)
   - ✅ Chrome на Android

### ✅ Результат:
- Приложение работает везде одинаково
- Анимации плавные
- Кнопки кликабельны
- Drag & Drop работает (на десктопе)

---

## ✅ 3. Тест Rate Limiting

### Что тестируем:
- Ограничение количества запросов
- Правильное отображение ошибок

### Как тестировать:

#### Шаг 1: Обычное использование
1. Загрузите видео
2. Нажмите "Анализировать"
3. Дождитесь результата
4. Нажмите "Создать новый сценарий"

**✅ Должно работать нормально**

#### Шаг 2: Проверка лимита
5. Повторите шаги 1-4 еще 4 раза (итого 5 запросов)
6. Попробуйте сделать 6-й запрос

**✅ Должно показать ошибку:**
```
Превышен лимит запросов (5 в 15 мин). 
Попробуйте через X мин.
```

#### Шаг 3: Проверка статуса
```javascript
// Откройте Console (F12)
import { videoAnalysisLimiter } from './lib/rateLimiter';
videoAnalysisLimiter.getStatus();

// Вывод:
// {
//   currentRequests: 5,
//   maxRequests: 5,
//   remainingRequests: 0,
//   windowMs: 900000,
//   resetTime: 1699632000000
// }
```

#### Шаг 4: Сброс (для тестирования)
```javascript
// В Console (F12)
localStorage.removeItem('ai_reels_video_analysis_limit');
location.reload();
```

### ✅ Результат:
- Лимит работает корректно
- Ошибки информативные
- Счетчик сбрасывается через 15 минут

---

## ✅ 4. Тест XSS защиты

### Что тестируем:
- Защиту от вредоносных скриптов
- Санитизацию пользовательских данных

### Тест 1: Санитизация имени файла

#### Как тестировать:
1. Создайте файл с именем: `<script>alert("XSS").mp4`
2. Попробуйте загрузить его
3. Проверьте отображаемое имя

**✅ Результат:** Имя должно быть безопасным (без `<script>`)

```javascript
// В Console проверьте:
import { sanitizeFilename } from './lib/sanitizer';
sanitizeFilename('<script>alert("XSS").mp4');
// Результат: "_script_alert__XSS__.mp4"
```

### Тест 2: Санитизация текста

```javascript
// В Console (F12)
import { sanitizeText } from './lib/sanitizer';

sanitizeText('<script>alert("XSS")</script>');
// Результат: "<script>alert(&quot;XSS&quot;)</script>"

sanitizeText('Обычный текст');
// Результат: "Обычный текст"
```

### Тест 3: Блокировка опасных URL

```javascript
// В Console
import { sanitizeUrl } from './lib/sanitizer';

sanitizeUrl('javascript:alert("XSS")');
// Результат: "" (пустая строка - заблокирован)

sanitizeUrl('https://safe-site.com');
// Результат: "https://safe-site.com" (разрешен)
```

### Тест 4: Валидация MIME типов

```javascript
// В Console
import { isValidVideoMimeType } from './lib/sanitizer';

isValidVideoMimeType('video/mp4');
// Результат: true ✅

isValidVideoMimeType('application/javascript');
// Результат: false ❌

isValidVideoMimeType('text/html');
// Результат: false ❌
```

### Тест 5: Content Security Policy

#### Как тестировать:
1. Откройте DevTools (F12)
2. Перейдите в Network tab
3. Обновите страницу
4. Найдите HTML документ (первый запрос)
5. Проверьте Response Headers

**✅ Должен быть заголовок:**
```
Content-Security-Policy: default-src 'self'; script-src 'self' ...
```

#### Тест блокировки:
```javascript
// Попробуйте выполнить в Console:
const script = document.createElement('script');
script.src = 'https://evil-site.com/malicious.js';
document.body.appendChild(script);

// ✅ Должна быть ошибка CSP в Console
```

### ✅ Результат:
- Все опасные данные санитизируются
- CSP блокирует внешние скрипты
- Приложение защищено от XSS

---

## 🔍 Дополнительные тесты

### Тест 1: localStorage санитизация

```javascript
// В Console
import { saveScript } from './lib/api';

const maliciousData = {
  title: '<script>alert("XSS")</script>',
  original: {
    transcription: '<img src=x onerror=alert(1)>',
    translation: 'Normal text'
  },
  keys: [],
  script: [],
  recommendations: []
};

saveScript(maliciousData);

// Проверяем сохраненные данные:
const scripts = JSON.parse(localStorage.getItem('ai_reels_saved_scripts'));
console.log(scripts[0].title);
// ✅ Должно быть безопасно: "<script>..."
```

### Тест 2: SQL Injection защита

```javascript
// В Console
import { detectSqlInjection } from './lib/sanitizer';

detectSqlInjection("SELECT * FROM users");
// Результат: true (обнаружена попытка инъекции)

detectSqlInjection("Обычный текст");
// Результат: false (безопасно)

detectSqlInjection("' OR '1'='1");
// Результат: true (обнаружена попытка инъекции)
```

### Тест 3: Глубокая санитизация

```javascript
// В Console
import { deepSanitize } from './lib/sanitizer';

const dirtyData = {
  title: '<script>alert(1)</script>',
  nested: {
    text: '<img src=x onerror=alert(1)>',
    array: [
      '<b>Bold</b>',
      { deep: 'javascript:alert(1)' }
    ]
  }
};

const clean = deepSanitize(dirtyData);
console.log(clean);
// ✅ Все поля должны быть санитизированы
```

---

## 📊 Чеклист тестирования

### Frontend безопасность:
- ✅ UTF-8 кодировка работает
- ✅ Кириллица отображается корректно
- ✅ Шрифты загружаются
- ✅ Rate limiting активен
- ✅ Лимит сбрасывается через 15 минут
- ✅ Санитизация имен файлов работает
- ✅ Санитизация текста работает
- ✅ URL валидация работает
- ✅ MIME типы проверяются
- ✅ CSP заголовок установлен
- ✅ localStorage данные санитизируются

### Браузеры:
- ✅ Chrome (последняя версия)
- ✅ Firefox (последняя версия)
- ✅ Safari (последняя версия)
- ✅ Edge (последняя версия)
- ✅ iOS Safari
- ✅ Android Chrome

### Адаптивность:
- ✅ Desktop (1920x1080)
- ✅ Tablet (768x1024)
- ✅ Mobile (375x667)

---

## 🐛 Что делать если тест не прошел

### Проблема: Кириллица отображается некорректно
**Решение:**
1. Очистите кеш браузера (Ctrl+Shift+Del)
2. Проверьте `/styles/globals.css` - должен быть импорт Google Fonts
3. Проверьте Network tab - шрифты должны загружаться

### Проблема: Rate limiting не работает
**Решение:**
1. Очистите localStorage
2. Проверьте Console на ошибки
3. Убедитесь что `/lib/rateLimiter.ts` импортирован в `/lib/api.ts`

### Проблема: Санитизация не работает
**Решение:**
1. Проверьте импорты в компонентах
2. Убедитесь что функции вызываются перед отображением
3. Проверьте Console на ошибки

### Проблема: CSP блокирует легитимные ресурсы
**Решение:**
1. Проверьте `/index.html` - CSP мета-тег
2. Добавьте домен в whitelist если нужно
3. Проверьте Console для точной ошибки CSP

---

## 📞 Получение помощи

Если тесты не проходят:
1. Проверьте Console (F12) на ошибки
2. Проверьте Network tab на проблемы с загрузкой
3. Посмотрите `/docs/SECURITY.md` для деталей
4. Проверьте все файлы из `/БЕЗОПАСНОСТЬ_ОБНОВЛЕНО.md`

---

## ✅ Финальная проверка

Если все тесты прошли:
- ✅ Приложение безопасно
- ✅ Кириллица работает
- ✅ Rate limiting защищает от спама
- ✅ XSS атаки блокируются
- ✅ Готово к использованию!

**Дата:** 10 ноября 2025  
**Статус:** Тесты готовы к запуску
