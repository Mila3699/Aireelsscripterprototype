# ✅ ЧЕКЛИСТ: Запуск AI Reels Scripter

## 🎯 ЧТО НУЖНО СДЕЛАТЬ

### ☐ 1. Получить API ключ (5 минут)
- [ ] Открыть https://makersuite.google.com/app/apikey
- [ ] Войти в Google аккаунт
- [ ] Нажать "Create API Key"
- [ ] Скопировать ключ (начинается с `AIzaSy...`)
- [ ] Сохранить в блокнот

### ☐ 2. Установить Node.js (5 минут)
- [ ] Скачать с https://nodejs.org/ (LTS версия)
- [ ] Установить
- [ ] Проверить в терминале: `node --version`
- [ ] Должно показать версию (например `v20.11.0`)

### ☐ 3. Создать backend папку (2 минуты)
```bash
mkdir ai-reels-backend
cd ai-reels-backend
```

### ☐ 4. Скопировать файлы backend (3 минуты)
Скопируйте из папки `/backend-example/`:
- [ ] `server.js`
- [ ] `package.json`
- [ ] `.env.example`

### ☐ 5. Настроить .env файл (2 минуты)
```bash
# Переименовать .env.example в .env
mv .env.example .env

# Открыть в текстовом редакторе и вставить ваш API ключ:
# GOOGLE_AI_API_KEY=AIzaSy...ваш_ключ
```

### ☐ 6. Установить зависимости (3 минуты)
```bash
npm install
```

### ☐ 7. Запустить backend (1 минута)
```bash
npm start
```

Должно показать:
```
🚀 AI REELS SCRIPTER - BACKEND
📡 Сервер запущен: http://localhost:3001
🔑 API ключ: ✅ Настроен
```

### ☐ 8. Проверить работу backend (1 минута)
Откройте в браузере: http://localhost:3001

Должно показать:
```json
{"status":"ok","service":"AI Reels Scripter Backend",...}
```

### ☐ 9. Подключить frontend к backend (3 минуты)

Откройте файл `/lib/api.ts`:

1. Найдите функцию `processVideo` (~строка 273)
2. **Закомментируйте** mock-версию (строки с `uploadVideo` и `analyzeVideo`)
3. **Раскомментируйте** продакшен-версию (блок с `fetch`)

Или просто замените всю функцию на:

```typescript
export async function processVideo(file: File): Promise<VideoAnalysisResult> {
  const formData = new FormData();
  formData.append('video', file);

  try {
    console.log('📤 Отправка видео на backend...');
    
    const response = await fetch(`${API_CONFIG.BACKEND_URL}/analyze`, {
      method: 'POST',
      body: formData,
    });

    if (!response.ok) {
      const errorData = await response.json().catch(() => ({}));
      throw new Error(errorData.error || `HTTP error! status: ${response.status}`);
    }

    console.log('✅ Получен ответ от backend');
    const result = await response.json();
    return result;
  } catch (error) {
    console.error('❌ Ошибка при отправке видео:', error);
    throw error;
  }
}
```

### ☐ 10. ТЕСТ! (2 минуты)

1. [ ] Backend запущен (терминал не закрыт)
2. [ ] Frontend открыт в браузере
3. [ ] Загрузить короткое видео (15-30 сек, MP4)
4. [ ] Нажать "Анализировать"
5. [ ] Подождать 30-60 секунд
6. [ ] Получить результат! 🎉

---

## 🎊 ГОТОВО!

Если все шаги выполнены, у вас работает **полнофункциональное приложение с AI**!

---

## 🆘 Если что-то не работает

### Backend не запускается
```bash
# Проверьте, что все зависимости установлены
npm install

# Проверьте .env файл
cat .env
# Должен содержать GOOGLE_AI_API_KEY=...
```

### "Cannot connect to backend"
1. Проверьте, что backend запущен
2. Откройте http://localhost:3001 в браузере
3. В `/lib/api.ts` проверьте `BACKEND_URL`:
   ```typescript
   BACKEND_URL: 'http://localhost:3001/api'
   ```

### "API key invalid"
1. Проверьте ключ в `.env` файле
2. Убедитесь, что скопировали полностью
3. Создайте новый ключ: https://makersuite.google.com/app/apikey

### Долго обрабатывается
- Используйте короткие видео (< 1 минуты)
- Проверьте интернет-соединение
- Посмотрите логи в терминале backend

---

## 📚 Дополнительные инструкции

- **Подробная инструкция:** `/ИНСТРУКЦИЯ_ДЛЯ_НОВИЧКОВ.md`
- **Backend документация:** `/backend-example/README.md`
- **Общая информация:** `/README.md`

---

## 💡 Советы

✅ Начинайте с коротких тестовых видео  
✅ Смотрите логи в терминале backend  
✅ Используйте видео с четкой речью  
✅ Не закрывайте терминал с backend  
✅ Сохраните API ключ в безопасном месте  

---

**Время на полную настройку: ~30 минут**

**Удачи!** 🚀
