# 🎯 ИНСТРУКЦИЯ ДЛЯ НОВИЧКОВ

## Что у вас сейчас есть?

У вас есть **красивое веб-приложение**, которое:
- ✅ Принимает видео через drag & drop
- ✅ Показывает анимацию обработки
- ✅ Выдает красивые результаты

**НО!** Оно работает с **примерами (mock-данными)**. Реального анализа AI пока нет.

---

## Почему так?

**Проблема:** Google AI API ключ нельзя использовать напрямую в браузере (это небезопасно).

**Решение:** Нужен **backend-сервер** (посредник), который:
1. Принимает видео от вашего сайта
2. Отправляет его в Google AI
3. Возвращает результат обратно на сайт

```
[ВАШ САЙТ] → [ВАШ BACKEND] → [GOOGLE AI] → [ВАШ BACKEND] → [ВАШ САЙТ]
```

---

## 🚀 ПЛАН ДЕЙСТВИЙ (5 шагов)

### ШАГ 1: Получите API ключ от Google (5 минут)

1. Откройте в браузере: https://makersuite.google.com/app/apikey
2. Войдите в Google аккаунт
3. Нажмите кнопку **"Create API Key"** (Создать API ключ)
4. Скопируйте ключ (он будет выглядеть так: `AIzaSyB...`)
5. Сохраните его в блокнот (понадобится в шаге 3)

**Важно:** Не показывайте этот ключ никому!

---

### ШАГ 2: Установите программы (10-15 минут)

Вам нужно установить 3 программы:

#### 2.1 Node.js (движок для backend)

**Windows/Mac:**
1. Скачайте с https://nodejs.org/ (выбирайте LTS версию)
2. Запустите установщик
3. Нажимайте "Далее" везде
4. Проверьте установку:
   - Откройте терминал (Command Prompt/Terminal)
   - Введите: `node --version`
   - Должно показать версию (например, `v20.11.0`)

**Mac через Homebrew:**
```bash
brew install node
```

#### 2.2 Git (система контроля версий)

**Windows:** https://git-scm.com/download/win  
**Mac:** Уже установлен, или `brew install git`

#### 2.3 Redis (для очередей задач)

**Windows:**
1. Скачайте Docker Desktop: https://www.docker.com/products/docker-desktop/
2. Установите и запустите
3. В терминале введите:
   ```bash
   docker run -d --name redis -p 6379:6379 redis:7-alpine
   ```

**Mac:**
```bash
brew install redis
brew services start redis
```

**Проверка Redis:**
```bash
redis-cli ping
# Должно вернуть: PONG
```

---

### ШАГ 3: Создайте backend (15-20 минут)

#### 3.1 Создайте папку для backend

```bash
# Откройте терминал и создайте папку
mkdir ai-reels-backend
cd ai-reels-backend
```

#### 3.2 Инициализируйте Node.js проект

```bash
npm init -y
```

#### 3.3 Установите библиотеки

```bash
npm install express cors dotenv multer @google/generative-ai
```

#### 3.4 Создайте файл `.env`

В папке `ai-reels-backend` создайте файл `.env` (просто `.env` без расширения) и вставьте:

```env
GOOGLE_AI_API_KEY=ВСТАВЬТЕ_СЮДА_ВАШ_API_КЛЮЧ
PORT=3001
FRONTEND_URL=http://localhost:3000
```

**Замените** `ВСТАВЬТЕ_СЮДА_ВАШ_API_КЛЮЧ` на ключ из Шага 1!

#### 3.5 Создайте файл `server.js`

В той же папке создайте файл `server.js` и скопируйте код (см. ниже):

<details>
<summary>📄 Нажмите, чтобы увидеть код для server.js</summary>

```javascript
require('dotenv').config();
const express = require('express');
const cors = require('cors');
const multer = require('multer');
const { GoogleGenerativeAI } = require('@google/generative-ai');

const app = express();
const PORT = process.env.PORT || 3001;

// Настройка CORS
app.use(cors({
  origin: process.env.FRONTEND_URL || 'http://localhost:3000',
  credentials: true
}));

// Настройка загрузки файлов (в память)
const upload = multer({
  storage: multer.memoryStorage(),
  limits: {
    fileSize: 100 * 1024 * 1024 // 100 MB
  }
});

// Инициализация Google AI
const genAI = new GoogleGenerativeAI(process.env.GOOGLE_AI_API_KEY);

// Промпт для анализа
const ANALYSIS_PROMPT = `
Ты — профессиональный SMM-аналитик и сценарист для коротких видео (Reels, TikTok).

Проанализируй загруженное видео и выполни следующие задачи:

1. ТРАНСКРИБАЦИЯ И ПЕРЕВОД:
   - Сделай полную транскрибацию аудиодорожки на языке оригинала
   - Переведи транскрибацию на русский язык

2. КЛЮЧИ К УСПЕХУ:
   Выяви 5 ключевых причин, почему это видео может быть успешным:
   - Хук (как привлечено внимание в первые 3 секунды)
   - Структура (как построен контент)
   - Подача (интонация, темп, энергетика)
   - Визуал (камера, монтаж, эффекты)
   - Аудио (музыка, звуковые акценты)

3. ГОТОВЫЙ СЦЕНАРИЙ:
   Создай пошаговый сценарий для создания аналогичного видео на русском языке.
   Для каждой сцены укажи:
   - Временной интервал (например, "0-3 сек")
   - Визуальный ряд (крупный план, средний план, демонстрация и т.д.)
   - Текст для озвучки (адаптированный под русский язык)
   - Заметка/совет (почему важен этот момент)

4. РЕКОМЕНДАЦИИ ПО СОЗДАНИЮ:
   Дай практические советы по:
   - Интонации и голосу
   - Фоновой музыке
   - Работе с ИИ-аватаром (если применимо)
   - Монтажу и эффектам

Ответ предоставь СТРОГО в формате JSON со следующей структурой:
{
  "original": {
    "transcription": "...",
    "translation": "..."
  },
  "keys": [
    {"title": "...", "description": "..."}
  ],
  "script": [
    {"time": "...", "visual": "...", "text": "...", "note": "..."}
  ],
  "recommendations": [
    {"category": "...", "text": "..."}
  ]
}
`;

// Главный эндпоинт для анализа
app.post('/api/analyze', upload.single('video'), async (req, res) => {
  try {
    if (!req.file) {
      return res.status(400).json({ error: 'Видео не загружено' });
    }

    console.log('📹 Получено видео:', req.file.originalname, `(${(req.file.size / 1024 / 1024).toFixed(2)} MB)`);

    // Инициализация модели Gemini
    const model = genAI.getGenerativeModel({ model: 'gemini-1.5-flash' });

    // Конвертация видео в base64
    const videoBase64 = req.file.buffer.toString('base64');

    console.log('🤖 Отправка в Google AI...');

    // Отправка запроса к Google AI
    const result = await model.generateContent([
      {
        inlineData: {
          mimeType: req.file.mimetype,
          data: videoBase64
        }
      },
      { text: ANALYSIS_PROMPT }
    ]);

    const response = await result.response;
    const text = response.text();

    console.log('✅ Получен ответ от Google AI');

    // Парсинг JSON из ответа
    let analysisData;
    try {
      // Убираем возможные markdown обертки
      const jsonText = text.replace(/```json\n?/g, '').replace(/```\n?/g, '').trim();
      analysisData = JSON.parse(jsonText);
    } catch (parseError) {
      console.error('❌ Ошибка парсинга JSON:', parseError);
      return res.status(500).json({ 
        error: 'Не удалось распарсить ответ от AI',
        raw: text 
      });
    }

    // Отправка результата
    res.json(analysisData);

  } catch (error) {
    console.error('❌ Ошибка:', error);
    res.status(500).json({ 
      error: 'Ошибка при обработке видео',
      message: error.message 
    });
  }
});

// Проверка здоровья сервера
app.get('/', (req, res) => {
  res.json({ 
    status: 'ok', 
    message: 'AI Reels Backend работает!',
    timestamp: new Date().toISOString()
  });
});

// Запуск сервера
app.listen(PORT, () => {
  console.log(`🚀 Backend сервер запущен на http://localhost:${PORT}`);
  console.log(`📡 Принимаю запросы от: ${process.env.FRONTEND_URL || 'http://localhost:3000'}`);
});
```
</details>

#### 3.6 Запустите backend

```bash
node server.js
```

Вы должны увидеть:
```
🚀 Backend сервер запущен на http://localhost:3001
📡 Принимаю запросы от: http://localhost:3000
```

**Отлично!** Backend работает. **НЕ ЗАКРЫВАЙТЕ** это окно терминала!

---

### ШАГ 4: Подключите backend к вашему сайту (5 минут)

Теперь нужно изменить файл `/lib/api.ts`, чтобы он использовал ваш backend вместо mock-данных.

**Откройте файл `/lib/api.ts`** и найдите функцию `processVideo` (строка ~273).

**Замените эту функцию:**

```typescript
export async function processVideo(file: File): Promise<VideoAnalysisResult> {
  // 1. Загружаем видео
  const fileId = await uploadVideo(file);
  
  // 2. Анализируем
  const result = await analyzeVideo(fileId);
  
  return result;
}
```

**На эту:**

```typescript
export async function processVideo(file: File): Promise<VideoAnalysisResult> {
  // Создаем FormData для отправки видео
  const formData = new FormData();
  formData.append('video', file);

  try {
    // Отправляем запрос к вашему backend
    const response = await fetch(`${API_CONFIG.BACKEND_URL}/analyze`, {
      method: 'POST',
      body: formData,
    });

    if (!response.ok) {
      throw new Error(`HTTP error! status: ${response.status}`);
    }

    const result = await response.json();
    return result;
  } catch (error) {
    console.error('Ошибка при отправке видео:', error);
    throw error;
  }
}
```

**Сохраните файл.**

---

### ШАГ 5: Тестируйте! 🎉

1. **Убедитесь, что backend запущен** (в терминале должно быть написано `Backend сервер запущен`)

2. **Откройте ваше приложение** в браузере (обычно http://localhost:3000 или через Figma Make)

3. **Загрузите короткое видео:**
   - Лучше всего начать с видео **15-30 секунд**
   - Размер до **10 МБ**
   - Формат **MP4**
   - С речью/озвучкой

4. **Нажмите "Анализировать"**

5. **Подождите 30-60 секунд**

6. **Получите РЕАЛЬНЫЙ анализ от Google AI!** 🎊

---

## 🎯 Что дальше?

### Если все работает:

✅ **Поздравляю!** Теперь у вас полностью рабочее приложение с AI!

**Следующие шаги:**
1. Попробуйте разные видео
2. Настройте промпт под свои нужды (в `server.js`)
3. Добавьте базу данных для хранения результатов
4. Выложите на продакшен (см. README.md)

### Если что-то не работает:

#### ❌ Ошибка "Failed to fetch"

**Причина:** Frontend не может подключиться к backend

**Решение:**
1. Проверьте, что backend запущен (`node server.js`)
2. Проверьте URL в `/lib/api.ts` (должно быть `http://localhost:3001/api`)

#### ❌ Ошибка "CORS"

**Причина:** Настройки безопасности

**Решение:**
Убедитесь, что в `server.js` правильный `FRONTEND_URL` в `.env`:
```env
FRONTEND_URL=http://localhost:3000
```

#### ❌ Ошибка "API Key invalid"

**Причина:** Неправильный или недействительный API ключ

**Решение:**
1. Проверьте `.env` файл в backend
2. Проверьте, что ключ скопирован полностью
3. Создайте новый ключ на https://makersuite.google.com/app/apikey

#### ❌ Долго обрабатывается (> 2 минут)

**Причина:** Большое или длинное видео

**Решение:**
- Используйте видео короче 1 минуты
- Сожмите видео (уменьшите разрешение)
- Попробуйте другое видео

---

## 📚 Полезные команды

### Остановить backend:
```bash
# В терминале, где запущен backend, нажмите:
Ctrl + C
```

### Перезапустить backend:
```bash
node server.js
```

### Проверить, работает ли backend:
```bash
# Откройте в браузере:
http://localhost:3001
# Должно показать: {"status":"ok",...}
```

### Посмотреть логи backend:
Просто смотрите в терминал, где запущен `node server.js`. Там будет:
- 📹 Какие видео загружаются
- 🤖 Когда отправляется в Google AI
- ✅ Когда получен ответ
- ❌ Если есть ошибки

---

## 💡 Советы

1. **Начинайте с коротких видео** (15-30 сек) для тестов
2. **Следите за квотами** Google AI (есть бесплатный лимит)
3. **Сохраняйте API ключ** в секрете
4. **Не закрывайте терминал** с backend во время работы
5. **Смотрите логи** в терминале, чтобы понимать, что происходит

---

## 🎉 Готово!

Теперь у вас **РЕАЛЬНО РАБОТАЮЩЕЕ** приложение с искусственным интеллектом!

**Вопросы?** Посмотрите другие файлы документации:
- `README.md` - общее описание проекта
- `docs/BACKEND_SETUP.md` - подробнее про backend
- `docs/FAQ.md` - частые вопросы

**Удачи!** 🚀
