# 📘 Пошаговая публикация AI Reels Scripter через Render + Supabase + Google AI

**Дата:** 11 ноября 2025  
**Статус:** Готово к публикации  
**Цель:** Запустить полноценную версию приложения с реальным Google Gemini AI анализом

---

## 🎯 Текущий статус

✅ **Frontend:** Готов (Figma Make проект)  
✅ **Backend:** Готов (/supabase/functions/server/index.tsx)  
✅ **Отдельный backend репо:** https://github.com/Mila3699/my-backend.git  
✅ **Supabase:** Настроен (projectId: ssqcxrimivxqdydgmfcn)  
✅ **Google AI API key:** Получен  
⏳ **Render:** Аккаунт создан, нужно подключить к GitHub

---

## 📋 ПЛАН ПУБЛИКАЦИИ

### **ШАГ 1: Подключение Render к GitHub** ⏱️ 3 минуты

#### 1.1. Войдите в Render
```
1. Откройте https://dashboard.render.com
2. Войдите в ваш аккаунт
```

#### 1.2. Подключите GitHub
```
1. В Dashboard → Settings → "GitHub"
2. Нажмите "Connect GitHub Account"
3. Разрешите Render доступ к вашим репозиториям
4. Выберите доступ:
   - Рекомендуется: "Only select repositories"
   - Выберите репозиторий: Mila3699/my-backend
5. Нажмите "Install & Authorize"
```

✅ **Результат:** Render теперь видит ваш backend репозиторий

---

### **ШАГ 2: Деплой Backend на Render** ⏱️ 10 минут

#### 2.1. Создайте Web Service
```
1. В Render Dashboard → "New +" (справа вверху)
2. Выберите "Web Service"
3. Выберите репозиторий: "my-backend"
4. Нажмите "Connect"
```

#### 2.2. Настройте Web Service

**Основные настройки:**
```
Name: ai-reels-backend
Region: Frankfurt (EU Central) - ближе к России
Branch: main (или master)
Root Directory: (оставить пустым)
Runtime: Node
```

**Build & Deploy настройки:**
```
Build Command: npm install
Start Command: node server.js
```

**Instance Type:**
```
Instance Type: Free (или Starter $7/мес для стабильности)
```

**⚠️ ВАЖНО:** Free tier может "засыпать" после 15 минут бездействия

#### 2.3. Установите Environment Variables

Нажмите "Advanced" → "Add Environment Variable"

Добавьте следующие переменные:

```env
# 1. Google AI API Key
GOOGLE_AI_API_KEY=ваш_google_ai_api_key_здесь

# 2. Порт (Render установит автоматически, но можно указать)
PORT=10000

# 3. CORS - разрешенные домены (пока оставьте *)
FRONTEND_URL=*

# 4. Node окружение
NODE_ENV=production
```

**Как получить Google AI API key (если забыли):**
```
1. Откройте https://aistudio.google.com/app/apikey
2. Нажмите "Create API key"
3. Выберите существующий проект или создайте новый
4. Скопируйте ключ (формат: AIzaSy...)
```

#### 2.4. Деплой!
```
1. Прокрутите вниз
2. Нажмите "Create Web Service"
3. Ждите 3-5 минут (смотрите логи деплоя)
```

✅ **Результат:** Backend будет доступен по URL типа:
```
https://ai-reels-backend.onrender.com
```

**Сохраните этот URL!** Он понадобится для frontend.

#### 2.5. Проверка работы Backend

Откройте в браузере (замените на ваш URL):
```
https://ai-reels-backend.onrender.com/
```

Должен вернуться JSON:
```json
{
  "status": "ok",
  "message": "AI Reels Scripter Backend",
  "version": "1.0.0",
  "timestamp": "2025-11-11T..."
}
```

Если видите это - **Backend работает!** ✅

---

### **ШАГ 3: Обновление Frontend для работы с Render Backend** ⏱️ 5 минут

**ВАЖНО:** Сейчас ваш frontend настроен на работу с Supabase Edge Functions.  
У вас есть **два варианта**:

#### ВАРИАНТ A: Использовать отдельный backend на Render (рекомендуется для Google AI)

Нужно обновить `/lib/api.ts` чтобы он вызывал Render вместо localhost:

**Текущая строка 408:**
```typescript
BACKEND_URL: 'http://localhost:3001/api',
```

**Изменить на:**
```typescript
BACKEND_URL: 'https://ai-reels-backend.onrender.com/api',
```

#### ВАРИАНТ B: Интегрировать Google AI в Supabase Edge Function

Добавить вызов Google AI прямо в `/supabase/functions/server/index.tsx`.

**Я рекомендую ВАРИАНТ A** для начала, так как:
- Быстрее запустить
- Проще отладить
- Можно масштабировать отдельно от Supabase

**Хотите, чтобы я обновил код для варианта A или B?**

---

### **ШАГ 4: Деплой Frontend** ⏱️ 10 минут

У вас есть **два варианта** для frontend:

#### ВАРИАНТ 1: Через Vercel (рекомендуется - быстрее и удобнее)

```bash
# 4.1. Скачайте код из Figma Make
# 4.2. Откройте терминал в папке проекта

# 4.3. Инициализируйте git (если еще не сделано)
git init
git add .
git commit -m "Initial commit"

# 4.4. Создайте репозиторий на GitHub
# Откройте https://github.com/new
# Название: ai-reels-scripter
# Публичный или приватный - на ваш выбор

# 4.5. Подключите к GitHub
git remote add origin https://github.com/Mila3699/ai-reels-scripter.git
git branch -M main
git push -u origin main

# 4.6. Откройте https://vercel.com
# Войдите через GitHub
# New Project → Import ai-reels-scripter

# 4.7. Настройте Environment Variables в Vercel:
VITE_SUPABASE_URL=https://ssqcxrimivxqdydgmfcn.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
VITE_BACKEND_URL=https://ai-reels-backend.onrender.com/api

# 4.8. Deploy!
```

#### ВАРИАНТ 2: Через Render Static Site

```bash
# В Render Dashboard:
1. New + → Static Site
2. Подключите frontend репозиторий
3. Настройте:
   Build Command: npm run build
   Publish Directory: dist
4. Добавьте Environment Variables (те же, что в Vercel)
5. Deploy
```

**Я рекомендую Vercel** - он быстрее и лучше оптимизирован для React/Vite.

---

### **ШАГ 5: Настройка CORS на Backend** ⏱️ 2 минуты

После деплоя frontend получите его URL (например, `https://ai-reels-scripter.vercel.app`)

#### 5.1. Обновите CORS в Render

```
1. Откройте Render Dashboard → ваш backend service
2. Environment → Edit
3. Найдите FRONTEND_URL
4. Измените с "*" на:
   FRONTEND_URL=https://ai-reels-scripter.vercel.app
5. Сохраните (backend перезапустится автоматически)
```

#### 5.2. Проверьте CORS в коде backend

Убедитесь, что в `/backend-example/server.js` (в вашем GitHub репо) есть:

```javascript
app.use(cors({
  origin: process.env.FRONTEND_URL || '*',
  credentials: true,
}));
```

Если нужно, обновите и запушьте изменения.

---

### **ШАГ 6: Интеграция Google AI в Backend** ⏱️ 15 минут

**ВАЖНАЯ ИНФОРМАЦИЯ:**  
Ваш текущий backend (`my-backend` репозиторий) должен содержать код для интеграции с Google Gemini AI.

#### 6.1. Проверьте наличие Google AI SDK

В `/backend-example/package.json` должно быть:

```json
{
  "dependencies": {
    "@google/generative-ai": "^0.19.0",
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "multer": "^1.4.5-lts.1",
    "dotenv": "^16.3.1"
  }
}
```

#### 6.2. Код интеграции в server.js

Проверьте, что есть примерно такой код:

```javascript
const { GoogleGenerativeAI } = require('@google/generative-ai');
const genAI = new GoogleGenerativeAI(process.env.GOOGLE_AI_API_KEY);

app.post('/api/analyze', upload.single('video'), async (req, res) => {
  try {
    const videoFile = req.file;
    const model = genAI.getGenerativeModel({ model: 'gemini-1.5-flash' });
    
    // Конвертируем видео в base64
    const videoBase64 = videoFile.buffer.toString('base64');
    
    // Отправляем в Gemini
    const result = await model.generateContent([
      {
        inlineData: {
          mimeType: videoFile.mimetype,
          data: videoBase64
        }
      },
      { text: ANALYSIS_PROMPT } // Промпт из frontend
    ]);
    
    const response = await result.response;
    const analysisData = JSON.parse(response.text());
    
    res.json(analysisData);
  } catch (error) {
    console.error('Analysis error:', error);
    res.status(500).json({ error: 'Ошибка анализа' });
  }
});
```

**Если кода нет - дайте знать, я помогу его добавить!**

---

### **ШАГ 7: Финальная проверка** ⏱️ 5 минут

#### 7.1. Проверьте все URL

```
✅ Backend Render: https://ai-reels-backend.onrender.com
✅ Frontend Vercel: https://ai-reels-scripter.vercel.app
✅ Supabase: https://ssqcxrimivxqdydgmfcn.supabase.co
```

#### 7.2. Протестируйте процесс

```
1. Откройте ваше приложение
2. Зарегистрируйтесь/войдите
3. Загрузите тестовое видео (до 100 МБ)
4. Нажмите "Анализировать"
5. Проверьте:
   ✅ Видео загружается
   ✅ Появляется индикатор обработки
   ✅ Конфетти показывается при первом результате
   ✅ Результат отображается корректно
   ✅ Кнопка "Сохранить" работает
   ✅ Сценарий появляется в "Сохранённые"
```

#### 7.3. Проверьте логи

**Backend логи в Render:**
```
1. Render Dashboard → ваш service → Logs
2. Должны видеть:
   📹 Получено видео: ...
   🤖 Отправка в Google AI...
   ✅ Анализ завершен
```

**Frontend логи в браузере:**
```
1. F12 → Console
2. Не должно быть красных ошибок
3. Должны видеть:
   📤 Подключение к backend...
   ✅ Backend подключен!
```

---

## 🎉 ГОТОВО!

Ваше приложение опубликовано и работает!

### 📊 Архитектура

```
┌─────────────────────────────────────────────────┐
│         Users (браузер)                         │
└──────────────────┬──────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────┐
│   Frontend (Vercel)                             │
│   https://ai-reels-scripter.vercel.app         │
│   - React + Vite                                │
│   - Apple UI дизайн                             │
│   - Drag & drop интерфейс                       │
└─────┬───────────────────────────────────┬───────┘
      │                                   │
      │ Auth & Storage                    │ Video Analysis
      ▼                                   ▼
┌──────────────────┐              ┌──────────────────┐
│  Supabase        │              │  Backend (Render)│
│  - Auth          │              │  Node.js + Express│
│  - Storage       │              │  Multer upload   │
│  - KV Database   │              └────────┬─────────┘
└──────────────────┘                       │
                                           │ API Call
                                           ▼
                                  ┌──────────────────┐
                                  │  Google AI       │
                                  │  Gemini 1.5 Flash│
                                  │  Video Analysis  │
                                  └──────────────────┘
```

---

## 🔧 Что делать дальше

### Оптимизация

1. **Кэширование результатов** - сохранять анализы в Supabase, чтобы не анализировать повторно
2. **Очередь задач** - для обработки нескольких видео параллельно
3. **Уведомления** - email при завершении анализа длинных видео
4. **Webhook** - интеграция с Telegram/Slack для уведомлений

### Мониторинг

1. **Render Metrics** - следить за использованием ресурсов
2. **Google AI Quota** - отслеживать лимиты API
3. **Error tracking** - Sentry для отлова ошибок
4. **Analytics** - Google Analytics или Plausible

### Монетизация

1. **Лимиты для бесплатных пользователей** - например, 5 анализов в день
2. **Premium план** - безлимитные анализы за $9.99/мес
3. **API доступ** - для разработчиков

---

## ❓ Частые вопросы

### Render backend "засыпает"?

**Проблема:** Free tier Render останавливается после 15 минут бездействия.  
**Решение:**
- Апгрейд до Starter ($7/мес)
- Или настроить cron ping каждые 10 минут

### Google AI возвращает ошибку квоты?

**Проблема:** Превышен лимит бесплатного плана (15 запросов/минуту).  
**Решение:**
- Добавить rate limiting на backend
- Перейти на платный план Google AI

### CORS ошибки?

**Проблема:** Frontend не может обращаться к backend.  
**Решение:**
- Проверьте FRONTEND_URL в Render environment variables
- Убедитесь, что URL совпадает с Vercel deployment URL

### Видео не загружается?

**Проблема:** Файл слишком большой или формат не поддерживается.  
**Решение:**
- Проверьте лимит в 100 МБ
- Поддерживаются: MP4, MOV, WEBM
- Добавьте логи на backend для отладки

---

## 🆘 Нужна помощь?

**Дайте мне знать на каком шаге вы сейчас, и я помогу!**

Что мне сделать прямо сейчас?

1. ✅ **Обновить код frontend** для работы с Render backend
2. ✅ **Проверить/обновить код backend** для интеграции Google AI
3. ✅ **Создать подробную инструкцию по деплою**
4. ✅ **Помочь с конкретным шагом**

**Напишите номер, и я сразу займусь этим!**
